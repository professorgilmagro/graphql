enum Status {
	active
	inactive
	blocked
}

type User {
	id: ID!
	name: String
	email: String
	gender: String
	age: Int
	salary: Float
	vip: Boolean
	status: Status
	profile_id: Int
	profile: Profile
}

type Profile {
	id: ID!
	name: String
}
